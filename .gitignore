# Python
__pycache__/
*.py[cod]
*.egg-info/
.Python

# Virtual environments
.venv/
venv/
env/

# Environment and secrets
.env
.env.local
*.pem
*.key

# Testing and type checking
.pytest_cache/
.coverage
coverage.xml
htmlcov/
.mypy_cache/
.ruff_cache/

# Editors
.vscode/
.idea/
*.swp

# Agent tooling. Machine-local, like the two above: how to run the servers belongs in
# README.md and CONTRIBUTING.md, where a person reads it, not in a launch config only one
# tool understands.
.claude/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Prototype archive (extracted contents are versioned under apps/web/)
*.zip
apps/web/.thumbnail

# Front end
node_modules/
dist/
*.tsbuildinfo

# Design briefs stay local — how the screens were produced, not part of the project.
docs/design-brief*.md

# Scratch file for `gh pr create --body-file`.
.pr-body.md
