# Tool configuration only.
#
# Dependencies live in requirements.in / requirements-dev.in and are pinned into
# requirements.txt / requirements-dev.txt by pip-compile. This project uses pip +
# pip-tools rather than declaring dependencies here, so pyproject.toml exists purely
# as the standard location for tool settings.

[tool.ruff]
line-length = 100
target-version = "py312"
src = ["."]

[tool.ruff.lint]
select = [
    "E",    # pycodestyle errors
    "W",    # pycodestyle warnings
    "F",    # pyflakes
    "I",    # isort
    "B",    # flake8-bugbear
    "UP",   # pyupgrade
    "C4",   # flake8-comprehensions
    "SIM",  # flake8-simplify
    "ARG",  # unused arguments
    "PTH",  # use pathlib
    "RUF",  # ruff-specific
]

ignore = [
    # UP017 rewrites datetime.timezone.utc to the datetime.UTC alias added in 3.11. They are
    # the same object. The older spelling is kept so every module stays importable under an
    # interpreter older than 3.11, which is what part of the verification tooling runs, and
    # nothing is gained by the newer name.
    "UP017",
]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["ARG"]  # fixtures are passed as unused-looking arguments

[tool.ruff.lint.isort]
# The alembic/ directory in this project makes ruff read the alembic package as local.
# It is a third-party dependency; without this, imports get sorted into the wrong section.
known-third-party = ["alembic"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-q --strict-markers"
markers = [
    "integration: requires a reachable PostgreSQL instance; deselect with -m 'not integration'",
]

[tool.mypy]
python_version = "3.12"
packages = ["syncaai"]
strict = true
warn_unreachable = true
plugins = ["pydantic.mypy"]

[[tool.mypy.overrides]]
module = "tests.*"
disallow_untyped_defs = false
